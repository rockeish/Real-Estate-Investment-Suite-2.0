{
  "name": "server",
  "version": "1.0.0",
  "description": "Backend for the Real Estate Portfolio Management Suite.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:init": "psql -U postgres -c \"CREATE DATABASE real_estate_portfolio;\" && psql -U postgres -d real_estate_portfolio -f db/database.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "author": "",
  "license": "ISC"
}
