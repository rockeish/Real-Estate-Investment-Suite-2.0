{
	"installCommand": "sh -c 'set -e; D=app; [ -d \"$D\" ] || D=../app; cd \"$D\" && npm ci'",
	"buildCommand": "sh -c 'set -e; D=app; [ -d \"$D\" ] || D=../app; cd \"$D\" && npm run build'",
	"cleanUrls": true
}